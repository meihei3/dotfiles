## Homebrew のインストール

Homebrew のインストールと、インストール済みのライブラリを出す

## Requirements

- [ ] "~/.Brewfile"が存在する事

## Usage

### インストール時

```
bash ./setup.sh
```

### インストールリストファイル作成

```
brew bundle dump --global
```
